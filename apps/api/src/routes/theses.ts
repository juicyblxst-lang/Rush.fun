import type {FastifyInstance} from "fastify"; import {z} from "zod"; import {getUser} from "../auth.js"; import {createThesis,listTheses} from "../services/thesis-service.js";
const schema=z.object({marketId:z.string().min(1),stance:z.enum(["bullish","bearish","neutral"]),title:z.string().min(3).max(160),body:z.string().min(10).max(10000)});
export async function thesisRoutes(app:FastifyInstance){app.get("/v1/markets/:marketId/theses",async request=>({items:await listTheses((request.params as {marketId:string}).marketId)}));app.post("/v1/theses",async(request,reply)=>{const user=await getUser(request);if(!user)return reply.code(401).send({code:"AUTH_REQUIRED",message:"Sign in to publish a thesis"});const parsed=schema.safeParse(request.body);if(!parsed.success)return reply.code(400).send({code:"VALIDATION",message:parsed.error.message});return createThesis(user.id,parsed.data);});}