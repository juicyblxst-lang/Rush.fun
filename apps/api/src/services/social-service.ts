import {adminDb} from "../db/client.js"; import {AppError} from "../lib/errors.js";
export async function createPost(userId:string,input:{marketId?:string;thesisId?:string;body:string}){if(!input.marketId&&!input.thesisId)throw new AppError("VALIDATION","A post must belong to a market or thesis");const {data,error}=await adminDb.from("posts").insert({author_id:userId,market_id:input.marketId??null,thesis_id:input.thesisId??null,body:input.body.trim()}).select("id,market_id,thesis_id,author_id,body,created_at,profiles:author_id(id,username,display_name,avatar_url,bio,created_at)").single();if(error)throw new AppError("DB_ERROR",error.message,500);return data;}
export async function listPosts(marketId:string){const {data,error}=await adminDb.from("posts").select("id,market_id,thesis_id,author_id,body,created_at,profiles:author_id(id,username,display_name,avatar_url,bio,created_at)").eq("market_id",marketId).order("created_at",{ascending:false}).limit(100);if(error)throw new AppError("DB_ERROR",error.message,500);return data??[];}
export async function addComment(userId:string,input:{postId?:string;thesisId?:string;body:string}){if((input.postId?1:0)+(input.thesisId?1:0)!==1)throw new AppError("VALIDATION","Exactly one comment target is required");const {data,error}=await adminDb.from("comments").insert({author_id:userId,post_id:input.postId??null,thesis_id:input.thesisId??null,body:input.body.trim()}).select("id,post_id,thesis_id,author_id,body,created_at,profiles:author_id(id,username,display_name,avatar_url,bio,created_at)").single();if(error)throw new AppError("DB_ERROR",error.message,500);return data;}