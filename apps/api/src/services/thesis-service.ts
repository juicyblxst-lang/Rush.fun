import {adminDb} from "../db/client.js"; import {AppError} from "../lib/errors.js";
export async function listTheses(marketId:string){const {data,error}=await adminDb.from("theses").select("id,market_id,author_id,stance,title,body,created_at,updated_at,profiles:author_id(id,username,display_name,avatar_url,bio,created_at)").eq("market_id",marketId).order("created_at",{ascending:false});if(error)throw new AppError("DB_ERROR",error.message,500);return data??[];}
export async function createThesis(userId:string,input:{marketId:string;stance:"bullish"|"bearish"|"neutral";title:string;body:string}){const {data,error}=await adminDb.from("theses").insert({market_id:input.marketId,author_id:userId,stance:input.stance,title:input.title.trim(),body:input.body.trim()}).select("id,market_id,author_id,stance,title,body,created_at,updated_at,profiles:author_id(id,username,display_name,avatar_url,bio,created_at)").single();if(error)throw new AppError("DB_ERROR",error.message,500);return data;}