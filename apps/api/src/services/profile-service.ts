import {adminDb} from "../db/client.js"; import {AppError} from "../lib/errors.js"; import {post as serializePost,profile as serializeProfile} from "../lib/serializers.js";
export async function getProfile(username:string){const result=await adminDb.from("profiles").select("id,username,display_name,avatar_url,bio,created_at").eq("username",username).maybeSingle();if(result.error)throw new AppError("DB_ERROR",result.error.message,500);return result.data?serializeProfile(result.data):null;}
export async function getProfilePosts(userId:string){const result=await adminDb.from("posts").select("id,market_id,thesis_id,author_id,body,created_at,profiles:author_id(id,username,display_name,avatar_url,bio,created_at),reactions:reactions(count),comments:comments(count),markets:market_id(external_id)").eq("author_id",userId).order("created_at",{ascending:false}).limit(50);if(result.error)throw new AppError("DB_ERROR",result.error.message,500);return (result.data??[]).map(row=>serializePost({...row,market_external_id:(row as any).markets?.external_id}));}