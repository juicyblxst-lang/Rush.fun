import {adminDb} from "../db/client.js"; import {AppError} from "../lib/errors.js"; import {verifyMessage} from "viem";
export async function linkWallet(userId:string,address:string,chain:"base",message:string,signature:`0x${string}`){if(!/^0x[a-fA-F0-9]{40}$/.test(address))throw new AppError("VALIDATION","Invalid EVM wallet address");const normalized=address.toLowerCase();const expected="RUSH.FUN wallet link\nAddress: "+normalized;if(message!==expected)throw new AppError("INVALID_SIGNATURE","Wallet proof message does not match");const valid=await verifyMessage({address:normalized as `0x${string}`,message,signature});if(!valid)throw new AppError("INVALID_SIGNATURE","Wallet signature could not be verified");const result=await adminDb.from("wallets").upsert({user_id:userId,address:normalized,chain,verified_at:new Date().toISOString()},{onConflict:"user_id,chain,address"}).select().single();if(result.error)throw new AppError("DB_ERROR",result.error.message,500);return result.data;}