import {marketProviders} from "../providers/index.js"; import {adminDb} from "../db/client.js";
export async function syncMarkets(){let count=0;const results=await Promise.allSettled(marketProviders.map(async provider=>{const result=await provider.listMarkets({limit:100});for(const market of result.items){const source=await adminDb.from("provider_sources").upsert({provider:market.provider,external_id:market.id,chain:market.chain,source_url:market.sourceUrl??null,metadata:{}},{onConflict:"provider,external_id"}).select("id").single();if(source.error)throw source.error;const asset=await adminDb.from("assets").upsert({provider_source_id:source.data.id,chain:market.chain,address:market.address,symbol:market.symbol,name:market.name,image_url:market.imageUrl??null,description:market.description??null,metadata:{}},{onConflict:"chain,address"}).select("id").single();if(asset.error)throw asset.error;const row=await adminDb.from("markets").upsert({provider_source_id:source.data.id,asset_id:asset.data.id,provider:market.provider,chain:market.chain,kind:market.kind,external_id:market.id,market_address:market.marketAddress??market.address,quote_symbol:market.quoteSymbol??null,price_usd:market.priceUsd??null,price_change_24h:market.priceChange24h??null,market_cap_usd:market.marketCapUsd??null,volume_24h_usd:market.volume24hUsd??null,liquidity_usd:market.liquidityUsd??null,source_url:market.sourceUrl??null,observed_at:market.updatedAt},{onConflict:"provider,external_id"});if(row.error)throw row.error;count++;}}));for(const result of results)if(result.status==="rejected")console.error("market sync provider failed",result.reason);return count;}