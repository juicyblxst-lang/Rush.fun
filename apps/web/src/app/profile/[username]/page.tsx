"use client";
import {useEffect,useState} from "react"; import type {Post,Profile} from "@rush/types"; import {api} from "../../../lib/api";
export default function ProfilePage({params}:{params:Promise<{username:string}>}){const [profile,setProfile]=useState<Profile|null>(null);const [posts,setPosts]=useState<Post[]>([]);useEffect(()=>{params.then(p=>api.profile(p.username).then(data=>{setProfile(data.profile);setPosts(data.posts);}).catch(()=>setProfile(null)));},[params]);if(!profile)return <main className="page"><div className="empty">Loading or profile not found.</div></main>;return <main className="page"><div className="eyebrow">Profile</div><h1>{profile.displayName}</h1><p className="muted">@{profile.username}</p>{profile.bio&&<p>{profile.bio}</p>}<h2>Posts</h2>{posts.length===0?<div className="empty">No posts yet.</div>:<div className="feed">{posts.map(p=><article key={p.id}><p>{p.body}</p><div className="muted">{new Date(p.createdAt).toLocaleString()}</div></article>)}</div>}</main>;}