"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import type {Market} from "@rush/types"; import {api} from "../../lib/api";
export default function Discover(){const [markets,setMarkets]=useState<Market[]>([]);const [q,setQ]=useState("");const [loading,setLoading]=useState(true);const load=()=>{setLoading(true);api.markets(q).then(x=>setMarkets(x.items)).catch(()=>setMarkets([])).finally(()=>setLoading(false));};useEffect(()=>{load();},[]);return <main className="page"><div className="section-head"><div><div className="eyebrow">Discover</div><h2>Markets people are watching</h2></div><input className="search" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&load()} placeholder="Search symbol or name"/></div>{loading?<p className="muted">Loading real market data…</p>:markets.length===0?<div className="empty">No markets available from the configured providers.</div>:<div className="grid">{markets.map(m=><Link className="card" key={m.id} href={"/markets/"+encodeURIComponent(m.id)}><div className="muted">{m.chain.toUpperCase()} · {m.provider}</div><h3>{m.symbol}</h3><div>{m.name}</div>{m.priceUsd!=null&&<div className="price">{"$"+m.priceUsd.toLocaleString(undefined,{maximumFractionDigits:6})}</div>}<div className="stat"><span>24h</span><span className={m.priceChange24h!=null&&m.priceChange24h<0?"red":"green"}>{m.priceChange24h!=null?m.priceChange24h.toFixed(2)+"%":"—"}</span></div></Link>)}</div>}</main>;}