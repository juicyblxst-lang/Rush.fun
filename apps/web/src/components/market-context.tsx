"use client";
import {useState} from "react"; import {supabase,supabaseConfigured} from "../lib/supabase"; import {api} from "../lib/api";
export function MarketContext({marketId}:{marketId:string}){const [text,setText]=useState("");const [busy,setBusy]=useState(false);const [message,setMessage]=useState("");return <section className="card" style={{marginTop:16}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",gap:12}}><div><b>Market context</b><p className="muted">A source-bounded summary of the market data and community arguments.</p></div><button className="pill" disabled={busy} onClick={async()=>{const {data}=await supabase.auth.getSession();if(!data.session){setMessage("Sign in to use market context.");return;}setBusy(true);setMessage("");try{const result=await api.context(marketId);setText(result.text);}catch(error){setMessage(error instanceof Error?error.message:"Could not generate context.");}finally{setBusy(false);}}}>{busy?"Thinking…":"Explain this market"}</button></div>{text&&<p>{text}</p>}{message&&<span className="muted">{message}</span>}</section>;}