import type {Market,MarketActivity,Thesis,Post} from "@rush/types";
const base=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000";
async function request<T>(path:string,init?:RequestInit):Promise<T>{const response=await fetch(base+path,{...init,headers:{"Content-Type":"application/json",...(init?.headers??{})},cache:"no-store"});const body=await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.message??response.statusText);return body as T;}
export const api={markets:(q?:string)=>request<{items:Market[]}>(q?"/v1/markets?q="+encodeURIComponent(q):"/v1/markets"),market:(id:string)=>request<Market>("/v1/markets/"+encodeURIComponent(id)),activity:(id:string)=>request<{items:MarketActivity[]}>("/v1/markets/"+encodeURIComponent(id)+"/activity"),theses:(id:string)=>request<{items:Thesis[]}>("/v1/markets/"+encodeURIComponent(id)+"/theses"),posts:(id:string)=>request<{items:Post[]}>("/v1/markets/"+encodeURIComponent(id)+"/posts"),createThesis:(token:string,input:unknown)=>request<Thesis>("/v1/theses",{method:"POST",headers:{Authorization:"Bearer "+token},body:JSON.stringify(input)}),createPost:(token:string,input:unknown)=>request<Post>("/v1/posts",{method:"POST",headers:{Authorization:"Bearer "+token},body:JSON.stringify(input)})};